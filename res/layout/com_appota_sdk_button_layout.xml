<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <FrameLayout
        android:id="@+id/chathead_image_frame"
        android:layout_width="@dimen/com_appota_sdk_button_size"
        android:layout_height="@dimen/com_appota_sdk_button_size" >

        <ImageView
            android:id="@+id/chathead_imageview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clickable="true"
            android:focusable="true"
            android:focusableInTouchMode="true" />
    </FrameLayout>

    <View
        android:id="@+id/chathead_margin_view"
        android:layout_width="match_parent"
        android:layout_height="50dip"
        android:layout_toLeftOf="@id/chathead_image_frame"
        android:background="@android:color/white"
        android:visibility="visible" />

    <LinearLayout
        android:id="@+id/chathead_buble_layer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/chathead_image_frame"
        android:orientation="vertical"
        android:paddingLeft="0dp"
        android:visibility="gone" >

        <TextView
            android:id="@+id/chathead_textview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="0dp"
            android:background="@android:color/white"
            android:textColor="@android:color/black"
            android:textSize="14sp"
            android:visibility="gone" />
    </LinearLayout>

</RelativeLayout>